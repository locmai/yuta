version: "3.0"

intents:
  - greet
  - goodbye
  - scale_deployment

entities:
  - name
  - namespace
  - replicas

responses:
  utter_greet:
  - text: "Hey! How are you?"
  - text: "Hi!"

  utter_goodbye:
  - text: "Bye"
  - text: "See ya!"

actions:
  - action_scale_deployment

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
